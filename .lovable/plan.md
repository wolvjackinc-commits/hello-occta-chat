Approved subject to these corrections only:

1. USE THE ACTUAL ACCEPTANCE FUNCTION

The current system uses:

`accept-contract-summary` with `journey_mode=true`

Do not introduce or reference a separate `journey-accept-contract` function.

Call `ensure-customer-from-accepted-contract` internally only after the existing journey-mode acceptance has completed successfully and the immutable CS PDF/hash exist.

2. CUSTOMER CREATION AND LOGIN SAFETY

At Contract Summary acceptance:

- create/reuse the customer profile;
- generate the account number;
- link the quote, CS, acceptance and journey;
- show the customer immediately in Customer 360.

Do not automatically mark a newly created email address as verified unless the existing journey has genuinely verified control of that email.

Do not store or log raw password-reset/magic-link URLs.

At final order submission, add one short-lived set-password/account-access link to the existing consolidated email. If the customer already has an account, use a dashboard/login link instead.

If the customer signs but does not complete the remaining journey, Customer 360 must show:

`Contract accepted — order completion pending`

Do not create a canonical order until final submission.

3. TWO DIFFERENT ORDER REFERENCES

Keep these as separate fields:

- `occta_order_number` — automatically generated by OCCTA
- `giacom_reference` — manually entered after Giacom issues it

The OCCTA order number should be available for staff to enter into Giacom as OCCTA’s customer/client reference.

Do not require `giacom_reference` merely to move to `ordered` if Giacom does not issue its reference immediately.

Transition to `ordered` should require:

- confirmation that the order was entered into Giacom;
- `entered_in_giacom_at`;
- the auto-generated OCCTA order number.

Allow the Giacom reference to be added immediately or later.

4. OVERVIEW RPC SECURITY

Do not use one RPC with a browser-supplied “admin/customer” mode.

Create separate server-authorised interfaces:

- `get_admin_customer_overview(account_number)` — staff role required
- `get_my_customer_overview()` — authenticated customer, derives user ID from JWT

The customer function must never return supplier references, supplier cost, margin, internal notes or activation references.

5. MANUAL FULFILMENT CREATION

Do not create fulfilment trackers as a side effect of opening Customer 360, Orders or Manual Fulfilment pages.

Create the tracker only through:

- a secure scheduled eligibility process after cooling-off; or
- an explicit idempotent admin action.

Page loading must remain read-only.

6. LEGACY STATUS MIRRORING

Do not copy canonical status text directly into legacy enum/status fields unless the value is supported.

Use an explicit compatibility mapping.

`lifecycle_status` is authoritative.

Legacy fields are temporary mirrors only and must not overwrite the canonical lifecycle.

7. SERVICE-LIVE TRANSACTION AND EMAIL

`confirm-service-live` should commit database state atomically, but email and invoice generation should be handled through durable outbox rows.

The activation transaction must not fail merely because the email provider is temporarily unavailable.

Guarantee:

- one service;
- one activation event;
- one activation-email outbox record;
- one first-billing job;
- no duplicate invoice on retry.

8. ACCOUNT NUMBER GENERATION

Generate `OCC########` through a database sequence or locked server-side allocator.

Do not use an unprotected random number generator followed by repeated retries.

The database unique constraint remains mandatory.

9. BACKFILL SAFETY

Run backfill in this order:

- dry-run report;
- reviewed batch;
- small production batches with checkpoints;
- final reconciliation report.

Do not process the entire production database in one unbounded transaction.

Backfill must send no emails and must not create services or start billing.

Proceed in the proposed order after applying these corrections.  
  
  
  
Goal

Wire the (already-working) public quote-to-order journey into a single canonical post-submit pipeline so Customer 360, Orders, Services, Billing, the customer dashboard and Manual Fulfilment all read/write the same records. No change to the public journey, Contract Summary, Worldpay, DD encryption or signed documents.

## Production safety rules (apply to every step)

- All lifecycle writes server-side, in transactional edge functions / `SECURITY DEFINER` SQL functions with role check + allowed-transition validation + idempotency + audit.
- Migrations backward-compatible: add columns, never drop; keep legacy `status` mirrored from new `lifecycle_status`; do not drop legacy enums or routes until backfill verified.
- Preserve existing IDs, signed PDFs, communications, invoices.
- No fake customers, no destructive backfill, no customer emails during backfill.
- Idempotency keys on every server action (e.g. `(order_id, action)` unique advisory or status guard).

## Technical plan

### 1. Schema migrations (additive)

```text
profiles            + account_number unique (already exists, ensure backfill)
orders              + journey_id, quote_id, contract_summary_id,
                      contract_acceptance_id, payment_method_id,
                      guest_order_id, customer_id (if missing),
                      lifecycle_status text, giacom_reference,
                      giacom_product_ref, entered_in_giacom_at,
                      expected_activation_date, actual_activation_date,
                      router_reference, internal_notes,
                      cancellation_requested_at, cease_date,
                      minimum_term_end_date, etf_policy_snapshot jsonb
guest_orders        + linked_order_id (preserve as audit/intake only)
order_journeys      + linked_customer_id (exists), order_id
contract_summaries  + customer_id backfill on acceptance
services            + order_id, journey_id, billing_anchor_day,
                      minimum_term_end_date, etf_policy_snapshot,
                      activation_reference
order_status_history (NEW append-only): order_id, previous_status,
                      new_status, changed_at, changed_by,
                      customer_note, internal_note, source,
                      giacom_reference, expected_activation_date,
                      actual_activation_date
admin_reconciliation_tasks (NEW): kind, severity, payload, status,
                      created_at, resolved_at, resolved_by
```

Plus a single canonical RPC `get_customer_overview(_account_number)` returning customer + order + service + billing + documents, with an admin variant exposing supplier/internal fields and a customer variant that strips them. Dashboard and Customer 360 both call this.

### 2. New / changed edge functions


| Function                                                 | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ensure-customer-from-accepted-contract` (NEW, internal) | Called from `journey-accept-contract` after CS acceptance. Idempotent: normalises email, finds existing auth user/profile, otherwise creates auth user (no email), creates profile, generates `OCC########` account number, links quote_request/quote/CS/acceptance/journey/payment_method/dd_intake, sets `order_journeys.customer_id` + `linked_customer_id`. Fail-closed + reconciliation task on duplicate-email collisions. **Does not send any email.** |
| `journey-submit-order` (MODIFY)                          | After payment-method step + final submit: creates one canonical `orders` row (idempotent on `journey_id`), generates internal OCCTA order number, links everything, writes `order_status_history` entry `→ order_received`, keeps `guest_orders` only as audit. The existing consolidated onboarding email keeps sending and we append the set-password magic-link block when the customer is newly created.                                                  |
| `order-lifecycle-transition` (NEW)                       | Single server entrypoint for admin transitions (`ordered`, `processing`, `committed`, `on_hold`, `cancellation_requested`, `cancelled`, `failed`). Validates required fields per transition (e.g. `ordered` requires `giacom_reference`, `committed` requires `expected_activation_date`). Writes audit + history row.                                                                                                                                        |
| `confirm-service-live` (NEW)                             | One atomic transaction: validates committed + giacom_ref + activation date + reference + confirm checkbox; upserts ONE `services` row from CS/billing snapshot; sets order `lifecycle_status=live`, service status, billing flags, next invoice date, minimum-term end date; writes history; queues first invoice / pro-rata; sends one service-live email; audits. Idempotent on `order_id`.                                                                 |
| `request-service-cancellation` (NEW)                     | Branches on contract type. Flex: cease = today + notice period from accepted CS; bills through cease; no ETF. Fixed: builds ETF preview from `etf_policy_snapshot` snapshot disclosed in accepted CS — if missing, marks `manual_compliance_review_required` and refuses to auto-confirm. Returns preview; admin confirms via second call to commit.                                                                                                          |
| `confirm-service-cancellation` (NEW)                     | Issues final invoice/credit note, stops billing, transitions to `cancelled` only after admin marks Giacom cease confirmed.                                                                                                                                                                                                                                                                                                                                    |
| `backfill-completed-journeys` (NEW, admin-only)          | Idempotent reconciliation: for each `completed` journey ensures customer + account + canonical order + links; never auto-creates a live service; queues `manual_review` for guest orders previously flagged `active`; emits a JSON report; sends NO customer emails.                                                                                                                                                                                          |
| `promote-guest-to-customer` (KEEP, deprecate UI)         | Stays as fallback for legacy guest_orders without a journey. AdminOrders button removed once backfill verified.                                                                                                                                                                                                                                                                                                                                               |


### 3. Frontend changes

**Customer 360 (`src/pages/admin/CustomerDetail.tsx`)** — default tab becomes **Operations** with the workflow card listed in the brief, action buttons that call `order-lifecycle-transition` / `confirm-service-live` / `request-service-cancellation`. Consolidate tabs to: Operations · Billing & Payments · Documents & Communications · Tasks & Notes · Account Details. Existing tab content preserved underneath.

**Admin global search + every cross-link** → route to `/admin/customers/{account_number}`. If account number missing, show reconciliation warning instead of routing by UUID. Files: header search, `Orders.tsx`, `Services.tsx`, `ManualFulfilment.tsx`, `Installations.tsx`, `Readiness.tsx`, `Tasks.tsx`.

**Manual Fulfilment (`src/pages/admin/ManualFulfilment.tsx`)** — replace paid-PR selector with order/journey eligibility query: `completed journey + accepted CS + immutable PDF + canonical order + start date + payment method + cooling-off passed + not cancelled`. Operate by `order_id`. Auto-create tracker when eligible. Remove "tracker active" mutation — UI now invokes `confirm-service-live`. Remove outdated paid-PR wording.

**Admin navigation (`src/components/admin/AdminLayout` / sidebar)** — restructure to the eight top-level sections in the brief, move Services/Installations/Readiness/Manual Fulfilment under Orders, fold Payments&DD/PaymentRequests under Billing, fold Chat Transcripts/KB under Support, fold Plans/Suppliers/Pricing under Products & Pricing. Hide Rewards/Referrals/Campaigns behind their platform feature flags. **Do not delete routes.**

**Customer dashboard (`src/pages/Dashboard.tsx` + tab files)** — collapse to: Overview · Order & Service · Billing & Payments · Documents · Support · Rewards (flagged) · Account. Order & Service merges Quotes/QuoteRequests/Orders/Services/Journey timeline using the same `get_customer_overview` RPC. Fix the bug that rewrites guest-order status to `pending` — show canonical `lifecycle_status` mapped to the customer-safe vocabulary (order_received/ordered/processing/committed/live). Strip supplier, supplier cost, supplier product code, margin, internal notes, activation reference from all dashboard responses.

### 4. Lifecycle vocabulary mapping

```text
lifecycle_status        legacy status sources                    customer-visible label
order_received          guest_orders.pending, submitted          Order received
ordered                 (new, requires giacom_reference)         Ordered
processing              (new)                                    Processing
committed               (new, requires expected_activation_date) Committed
live                    services.active, orders.active           Live
on_hold                 (new)                                    On hold
cancellation_requested  (new)                                    Cancellation requested
cancelled               cancelled                                Cancelled
failed                  failed                                   Action needed
```

Legacy `status` columns retained as compatibility mirrors, updated by trigger from `lifecycle_status` until removal.

### 5. Backfill + verification

1. Run `backfill-completed-journeys` in dry-run, review report.
2. Run for real; reconciliation tasks land in admin queue.
3. Manual review queue for previously-"active" guest orders (no auto service creation).
4. Verify the brief's section 16 checklist against real production records (no fake data).
5. Only after verification, hide deprecated admin nav entries.

### 6. Out of scope (will not touch)

Public journey UI, Contract Summary generation/PDF/acceptance, start-date step, payment-method step, Worldpay HPP / webhook, DD encryption, cooling-off cancellation flow, the consolidated onboarding email template (only the magic-link block is appended when newly creating an auth user).

## Deliverables on completion

Migrations list, edge-function list, route/component diff, customer-creation + account-number verification, status-transition matrix, service-live transaction summary, Flex vs Fixed cancellation rules, dashboard/admin parity check, nav before/after, backfill report, idempotency proof (double-click test), supplier-leak audit, build result.

---

Confirm and I'll switch to build mode and execute in this order: (1) migrations, (2) `ensure-customer-from-accepted-contract` + journey-submit-order wiring, (3) `order-lifecycle-transition` + `confirm-service-live`, (4) Customer 360 Operations tab + link fixes, (5) Manual Fulfilment rewrite, (6) dashboard collapse + status fix, (7) admin nav restructure, (8) cancellation/ETF flows, (9) backfill + report.
## Phase 4 corrections + Phase 5 — complete

### Phase 4 corrections
- `confirm_service_live_tx` now writes the `service_activation_outbox` AND `first_billing_jobs` rows inside the same SECURITY DEFINER transaction; any failure rolls activation back.
- Pro-rata logic rewritten in Postgres NUMERIC (no JS floats). New columns on `first_billing_jobs`: `period_start`, `period_end`, `billable_days`, `full_cycle_days`, `calc_method`. Activation exactly on anchor day → full cycle, not pro-rata.
- Monetary amounts stored as integer minor units (`amount_minor`).
- Function EXECUTE revoked from PUBLIC/anon/authenticated; service_role only.
- Edge function no longer dual-writes outboxes.

### Phase 5 — admin navigation + account-number routing
- Created `src/lib/adminLinks.ts` (`customerDetailHref`, `customerDetailHrefOrReconciliation`).
- `CustomerDetail.tsx` now: (a) silently redirects legacy UUID bookmarks to the account-number route, (b) shows "Account reconciliation required" card when no account number exists.
- Replaced UUID-based customer routes in:
  - `AdminLayout` global search + account-number lookup effect
  - `GlobalSearch` (already used account_number)
  - `PaymentRequests` "Open Customer" dropdown
  - `CustomersNeedingAttentionQueue`, `ComplianceIssuesQueue`
  - `CustomerQuickActions` (no UUID fallback)
- AdminLayout sidebar reorganised into exactly 8 sections: Overview / Sales / Customers / Orders / Billing / Support / Products & Pricing / Settings & Compliance. Rewards, Referrals and Campaigns hidden behind `VITE_FEATURE_REWARDS|REFERRALS|CAMPAIGNS` flags (default off).
- No routes deleted in `App.tsx` — every legacy bookmark still resolves.

## Phase 6 — Manual Giacom Tracking rewired around canonical orders

### Migrations
- `manual_fulfilment_orders.order_id` (FK orders) + unique partial index → 1 tracker per order.
- `can_create_manual_fulfilment_for_order(_order_id)` eligibility checker.
- BEFORE-INSERT trigger now accepts the order_id path (legacy PR/journey paths preserved).
- `create_manual_fulfilment_tracker_for_order(_order_id,_actor,_notes)` SECURITY DEFINER, idempotent. EXECUTE service_role only.
- View `manual_fulfilment_eligible_orders` (security_invoker) — canonical orders meeting all eligibility rules, joined with any existing tracker.
- Legacy backfill: linked trackers via journey_id (or unique CS match); unlinkable trackers raised high-priority `admin_reconciliation_tasks` (legacy rows untouched).

### Edge function
- `manual-fulfilment-create-tracker` — staff-only, idempotent wrapper around the RPC. Writes audit_logs entry.

### UI
- `src/pages/admin/ManualFulfilment.tsx` fully rewritten. Lists rows from `manual_fulfilment_eligible_orders`. No paid-PR selector, no webhook_verified requirement, no direct DB status writes.
- Action dialog: Create tracker · Record order entered into Giacom (→ ordered) · Add/update Giacom reference · Mark processing · Mark committed (requires expected activation date + note) · Put on hold · Resume · Start cancellation · Add note · Confirm service live (only when lifecycle == committed).
- Every status change calls `order-lifecycle-transition`. Activation calls `confirm-service-live`. No status dropdown that mutates the table.
- Customer 360 link uses account number; reconciliation marker when missing.
